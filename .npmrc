# npm registry configuration
registry=https://registry.npmjs.org/

# Use npm for publishing (not pnpm registry)
@studioantipodes:registry=https://registry.npmjs.org/

# Engine strict mode (enforce Node.js version)
engine-strict=true

# Hoist dependencies
shamefully-hoist=true

# Authentication token (for CI/CD)
# In CI: This is set via environment variable NPM_TOKEN
# Locally: Set by running `npm login`
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
